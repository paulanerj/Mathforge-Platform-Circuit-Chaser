# TEMPLATE

Copy this whole directory into `candidates/<your-name>/`, then:

1. Rename `BRAIN_TEMPLATE` and change its `id` and `label`.
2. Replace the `IDLE` branch with your idea.
3. Add it to `../candidates/index.ts`.
4. `npm test && npm run fixtures <YOUR_ID> P0_PRODUCTION`

The template compiles, runs and loses. Get it appearing in the dropdown first,
then make it clever.
